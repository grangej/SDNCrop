import XCTest
@testable import SDNCrop

final class SDNCropTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SDNCrop().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
